{
  "name": "agent-context-template",
  "version": "1.0.0",
  "description": "Agent-First Project Context System with MCP support",
  "scripts": {
    "validate-mcp": "node scripts/validate-mcp-schemas.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}
